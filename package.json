{
  "name": "@pact/engine",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "pact": "src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "pact": "bun run src/cli.ts",
    "build": "bun build src/index.ts --outdir dist --target node"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
